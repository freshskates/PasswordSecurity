#include "pch.h"
#include "../PasswordSecurity/src/panel/panel.h"
#include <gtest/gtest.h>

TEST(PanelTest, Startup) {
	Panel test; 
	//test.run();
	EXPECT_EQ(12, 22);
	EXPECT_TRUE(false);
}

TEST(TestCaseName, TestName1) {
	//Panel test; 
	//test.run();
	EXPECT_EQ(1, 1);
	EXPECT_TRUE(true);
}

TEST(TestCaseName, TestName2) {
	//Panel test; 
	//test.run();
	EXPECT_EQ(1, 1);
	EXPECT_TRUE(true);
}

TEST(TestCaseName, TestName3) {
	//Panel test; 
	//test.run();
	EXPECT_EQ(1, 1);
	EXPECT_TRUE(true);
}

